<p>
  <strong>Commenter:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @article.text %>
</p>

<h1> Comments </h1>
<ul>
  <%= @atricle.comments.each do |c| %>
<li>Commenter :<%= comment.commenter %> </li> <br>
<li>Body :<%= comment.body %> </li>
<li> <% link_to "å", article_comment_path (comment) </li>
<% end %>
</ul>

<% end %>
<h2>Add a comment:</h2>
<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :commenter %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
